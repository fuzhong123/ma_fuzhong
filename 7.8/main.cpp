#include <iostream>
#include <array>
using namespace std;

int main()
{

    )a
    array <int ,7>alphabet={1,2,3,4,5,6,7};
    cout<<"array对象元素6的值为 :"<<alphabet[6]<<endl;


    )b
    array<float,5>grades={2.0,5.6,11.5,20.2,13.5};
    float x;
    cout<<"请输入一个值";
    cin>>x;
    grades[4]=y;






    )c
    array<int ,5>values={8,8,8,8,8};







    )d
    double x=0.0;
    double a=0.0
    array<double ,100>temperatures;
    for(int i=0;i<100;i++)
    {
        temperatures[i]=a;
        a++;
    }
    for(double temperature:temperatures)
    {
       x+=temperature;
    }
    cout<<"这100个元素的和为:"<<x<<endl;




    )e
    不会







    )f
    array<double,99>w;
    for(double a:w)
    {
        if(a<w[0])
        w[0]=a;
    }
    cout<<"w中的最大值为 :"<<w[0]<<endl;
    for(double a:w)
    {
        if(a>w[0])
            w[0]=a;
    }
    cout<<"w中的最小值为 :"<<w[0]<<endl;
    return 0;
}
